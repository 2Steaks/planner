/** @format */

import React, { FunctionComponent } from 'react';
import { History, PrivateLayout } from '@project/components';

const HistoryPage: FunctionComponent = () => (
  <PrivateLayout>
    <History />
  </PrivateLayout>
);

export default HistoryPage;
