/** @format */

import React, { FunctionComponent } from 'react';
import { PublicLayout, Login } from '@project/components';

const LoginPage: FunctionComponent = () => (
  <PublicLayout>
    <Login />
  </PublicLayout>
);

export default LoginPage;
