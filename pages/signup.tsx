/** @format */

import React, { FunctionComponent } from 'react';
import { PublicLayout, SignUp } from '@project/components';

const SignUpPage: FunctionComponent = () => (
  <PublicLayout>
    <SignUp />
  </PublicLayout>
);

export default SignUpPage;
